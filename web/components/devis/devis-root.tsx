"use client";

import { DevisWizard } from "./devis-wizard";

/** Surface globale du configurateur de devis, montée une fois dans le layout. */
export function DevisRoot() {
  return <DevisWizard />;
}
